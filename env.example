# PrepPal Waitlist Environment Variables

# Server Configuration
NODE_ENV=development
PORT=3001

# MongoDB Configuration
# For local development (if you have MongoDB installed locally):
# MONGODB_URI=mongodb://localhost:27017/preppal_waitlist

# For MongoDB Atlas (recommended for production):
# Replace <username>, <password>, and <cluster-url> with your actual values
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/preppal_waitlist?retryWrites=true&w=majority

# For development, you can use this free MongoDB Atlas cluster:
MONGODB_URI=mongodb://localhost:27017/preppal_waitlist

# Optional: Analytics and Monitoring
# GOOGLE_ANALYTICS_ID=
# SENTRY_DSN=

# Optional: Email Service (for sending notifications)
# SENDGRID_API_KEY=
# FROM_EMAIL=noreply@preppal.ai
